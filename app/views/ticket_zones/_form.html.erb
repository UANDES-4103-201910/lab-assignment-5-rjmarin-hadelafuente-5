<%= form_with(model: ticket_zone, local: true) do |form| %>
  <% if ticket_zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_zone.errors.count, "error") %> prohibited this ticket_zone from being saved:</h2>

      <ul>
      <% ticket_zone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
